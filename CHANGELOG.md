# Changelog

## v4.1.1（2026-09-15）

把「和我的思维导图同步」这条路上还没接通的几个口子补齐——目标只有一个：
**在日历里做的删除和勾选，对端一定收得到，不会被下一次同步又还原回去。**

### 🔁 复习任务「删了又回来」的剩余入口
v3.3.0 修的是右键删除那一条路，这次补齐其余几条：
- **批量删除也会通知对端**：右侧「逾期未完成 / 未完成 / 已完成」三组的删除按钮
  （一键删除逾期任务、删除本周未完成任务、删除本周已完成任务）以前只删本地、不通知对端，
  这些组里混着的复习任务下一次同步会被原样重建。现在与单条删除走同一条通知。
- **「清空当天任务」和设置里的「一键删除所有任务」同样会通知对端**（这两条也是这次补上的）。
- HTTP API 与 MCP 的删除**本来就有**通知，这次没动。

### ✅ 勾选完成 / 取消完成后立刻回推对端
- 新增「复习任务状态变化」通知：**单条勾选**、**批量「标记为已完成 / 还原为未完成」**、
  以及 **HTTP API / MCP 的完成接口**，四条路都会立刻把新状态与「状态最后变更时间」推给
  my-mindmap agent（对端按时间戳仲裁，谁新听谁的）。
- 以前只有界面上的单条勾选会即时回推，批量按钮和 API 得等下一次定时同步（最长一小时）才对齐——
  表现就是「我在这边全勾完了，那边还是没勾」。

### 🧪 回归测试
- 新增 4 项：清空当天 / 全部清空 / 一键删逾期 / 一键标记逾期完成 各自的「对端通知」行为。
- 跨平台核心测试 109 项、Windows 专属测试 27 项，全部通过。

