# Changelog

## v3.1.6（2026-09-03）

### 🐛 修复

- **复制 MCP / Skill / Token 卡顿并偶发报错 0x800401D0**：剪贴板被其它进程短暂占用时
  （`CLIPBRD_E_CANT_OPEN`），原来在 UI 线程上用 `Thread.Sleep` 同步重试，既卡界面又容易
  重试耗尽。现在改为在 UI 线程上异步重试（最多 15 次），不再阻塞界面，几乎总能复制成功。
- **新增单实例运行**：重复双击启动多个副本没有意义。现在使用命名互斥锁确保同一用户会话
  下只有一个实例；再次启动时会把已运行实例的窗口唤回前台，然后退出新进程。

## v3.1.5（2026-09-01）

### 🐛 修复

- **设置保存必弹错误框**：`SaveCore()` 被历史代码调用了两次，第一次已关闭窗口，第二次再设 `DialogResult` 抛
  "只能在创建 Window 并且作为对话框显示之后才能设置 DialogResult"。现在只调用一次。
- **嵌入桌面保存后立即生效**：之前勾选"嵌入桌面/锁定"后提示"重启才生效"，用户勾完没反应误以为坏了。
  现在保存即应用（开启立即沉底并启动看门狗，取消立即恢复普通窗口），无需重启。
- 看门狗 Tick 事件只挂载一次，避免嵌入开关反复切换时累积多个定时处理器。

### 🎨 界面

- "本周任务完成情况 → 已完成"组的"标记为未完成"按钮图标由 ✗（橙色）改为 ↺（蓝色），
  与同组删除按钮 ✕（红色）明显区分，避免误点删除。

## v3.1.4（2026-09-01）

### 🐛 修复：左右侧任务状态不同步（本次核心问题）

- **修复「右侧勾选完成 / 取消完成，左侧月份小格子不更新」**：日历格子里的任务
  ViewModel 与右侧面板的是**不同实例**，而底层任务对象是 POCO、不发出任何变更通知。
  之前 `DayCellViewModel.Refresh` 的"无结构变化早返"路径跳过了状态同步，导致在右侧
  改动完成状态后，左侧小格子永远停在旧状态。现在每条刷新路径都会把最新状态推给所有
  视图实例（含 `TaskItemViewModel.SyncFromModel` 快照同步）。
- **修复「取消已完成，任务不回未完成分组」**：右侧面板的分组改用两个显式的
  `ObservableCollection`（未完成 / 已完成）由 ViewModel 直接搬运，替换掉
  `CollectionViewSource + PropertyGroupDescription` 分组 —— 后者对"同一批条目属性
  变化要换组"支持极差，是取消完成不挪回未完成、勾选完成不挪到已完成的根因。
- **修复「历史逾期任务勾完成会从三个本周分组里同时消失」**：本周"已完成"组的判定
  从"计划日期在本周"放宽为"计划日期在本周 **或** 实际完成时刻在本周"的并集。
- **修复「一键清空当天任务，左侧月份小格子残留残影」**：清空操作改为走完整
  `RebuildCalendar`，而不是只刷新右侧面板。

### ✨ 新增：任务时间追踪与悬浮信息

- 任务模型新增时间指标：**未完成天数**（创建至今）、**逾期天数**（过计划日未完成）、
  **完成用时**（完成时刻 - 创建时刻）、**超时完成天数**（完成晚于计划日）。
- 鼠标悬浮在任意任务上（日历小格子 / 今日任务 / 本周三组），Tooltip 会显示：
  创建时间、完成时间、用时、超时情况；未完成的任务显示已拖多少天、逾期多少天。
- 今日任务面板与本周任务列表新增**时间徽标**（如"逾期 2 天""用时 3 天"）。
- 加载旧数据时自动补全自洽性：已完成但没有完成时间的任务，补一个兜底时间戳。

### ✨ 新增：定时报告（任务完成情况周报 / 月报）

- 设置面板新增「定时报告」：可配置每周几 / 每月几号 + 发送时刻，推送到
  飞书 / 企业微信 / 自定义 webhook（按企微 text 格式，可接 Server 酱 / PushPlus
  等转发到个人微信）。
- 报告内容：完成率、平均用时、**耗时最长**、**完成最快**、**超时完成**、
  **至今未完成（按拖延时长排序）**、**逾期未完成**。
- 按"周期键"做幂等标记：同一周期只发一次，支持开机补发；渠道失败自动重试
  （每周期最多 5 次），不会刷屏。

### 🔒 安全修复（本地 HTTP 服务加固）

- **高危：MCP 端点可能完全无鉴权** —— Token 的生成原本只挂在 API 服务启动里，
  API 关闭、MCP 单独开启时 Token 为空，`/mcp` 直接裸奔，任何网页可跨域增删改任务。
  现在 Token 在任何 HTTP 服务启动前统一生成；Token 为空时两个端点都**拒绝**服务。
- **CORS 收敛**：`Access-Control-Allow-Origin` 从 `*` 改为只回显本机 origin
  （localhost / 127.0.0.1 / [::1] / null），非本机 origin 直接 403，防任意网页跨源调用。
- **Token 比较**改用 `CryptographicOperations.FixedTimeEquals` 常时间比较。
- **请求体上限 1 MB**（含无 Content-Length 的兜底），防超大 body 耗尽内存。
- 未配置 webhook 渠道时提醒推送失败不再静默吞掉，改为写入日志。

### 🧹 其它健壮性

- 配置落盘改为「临时文件 + 原子 rename」，避免写一半崩溃导致 token / webhook 静默丢失。
- 自动备份目录新增轮转：默认保留最近 30 份，不再无限累积。
- 修正一处过时的单元测试断言（周视图格子高度已改为独立可调，不再跟随月视图）。
- 新增 5 个回归测试覆盖：右侧切换同步到小格子、完成/取消在分组间移动、
  逾期任务完成不消失、清空当天任务清掉小格子残影、时间指标计算。
  当前共 **45 个单元测试全部通过**。

### ⚠️ 说明

- 本轮不涉及界面主题 / 布局调整，原有主题与交互保持不变。
