# Changelog

## v3.3.4（2026-09-15）

修掉「不管选哪种背景模式，日期格子和右侧任务列表永远是纯白底」——Avalonia 宿主从来没有实现过换肤。

### 🎨 背景模式只换了外壳，格子和右侧面板一直是纯白
- **根因（如实说明）**：WPF 宿主有一套完整的 `ApplyBackgroundResources()`，会按背景模式整体替换约 28 个命名画刷
  （日期格子 / 任务胶囊 / 右侧面板 / 本周分组卡片 / 文字 / 边框 / 顶栏按钮）。v3.2.0 起安装包换成 Avalonia 宿主后，
  这套调色板**没有跟着搬过来**：Avalonia 侧的颜色是写死在 XAML 里的十六进制值和 `CellPalette` 静态常量，
  切换背景模式只改了外壳 Shell 那一层底色。于是底色已经是暗色 / 蓝色 / 薄荷，格子和右侧任务列表却还是纯白。
- **现在怎么做**：
  1. XAML 里所有颜色改成 `{DynamicResource 键名}`，键名与 WPF 宿主完全一致；状态差异（非本月 / 今日 / 选中 /
     重要任务 / 补班日）改用 Avalonia 的条件样式类 `Classes.xxx="{Binding ...}"`，不再走会被缓存的转换器
     —— 转换器的结果被 Avalonia 缓存住，正是「换了模式刷不动」的另一半原因。
  2. 把 `ApplyBackgroundResources()`（深色 / 无背景 / 各浅色模式三个分支）搬到 Avalonia 宿主，切模式时整体替换
     资源：日期格子、任务胶囊、右侧面板、本周分组卡片、年视图月份块、节假日徽标、文字与边框一起换色。
  3. 深色模式同时把 FluentTheme 的控件主题切到 Dark（下拉框 / 滑杆 / 输入框 / 勾选框不再在深色底上发白），
     浅色模式显式钉死 Light，不再跟随系统主题。
  4. 「无背景」模式下格子改为整块透明（与 WPF 宿主一致），只留文字和任务胶囊。
- **浅色外观保持原样**：浅色分支的取值刻意与改动前的十六进制值逐一对应，新增的只是按模式着色这件事本身。

### ✅ 测试
- 跨平台单测 79 例 + Windows 专测 27 例 = **106 例全部通过**。
- 另用同一份合成数据、同一窗口尺寸，对改动前 / 改动后的两个构建逐张截图对比：暗色磨砂下日期格子与右侧任务面板
  已变为深色、深色文字变浅色；「纯色」模式下与改动前基本一致（顶栏按钮由系统默认样式换成外壳同色系按钮）。
## v3.3.3（2026-09-15）

修掉 v3.3.2 暴露出来的真问题：窗口请求了系统材质（Mica），把外壳整层盖住 —— 四角变直角、透明度滑杆失效。

### 🩹 毛玻璃模式下四角变直角 / 透明度调不动 / 底下多一层白底
- **根因**：v3.3.2 把窗口改成无边框之后，`TransparencyLevelHint` 里请求的 `Mica` 才真的会被 Windows 应用
  （带系统标题栏时这条请求是拿不到的）。Mica 是操作系统在窗口最底层画的一层**不透明系统材质**，于是：
  圆角外壳被压在它上面看不见 → 四个角看起来是直角；外壳里再叠一层也盖不住它 → 透明度滑杆怎么拉都有白底。
  实测确认：毛玻璃模式下系统实际给的就是 `Mica`，而「透明」模式下是 `Transparent`（所以只有毛玻璃模式出问题）。
- **现在怎么做**：不再向系统要任何材质，窗口固定请求「逐像素透明」——底色 / 22px 圆角 / 1px 细边框全部由
  外壳自己画，与 v3.1.x 的 WPF 宿主（`WindowStyle=None` + `AllowsTransparency`）完全同一套观感。
  这样四个角是真圆角（圆角外沿能透出桌面），透明度滑杆恢复生效，也不再有多余的白底。
- **只有这一处改动**，其余（无标题栏、拖动、边缘缩放、双向删除、同步等）均维持 v3.3.2 的行为。

### ✅ 测试
- 跨平台单测 79 例 + Windows 专测 27 例 = **106 例全部通过**。
- 另以屏幕取色实测确认：毛玻璃模式下窗口四角像素与窗口外背景一致（真圆角 + 真透明），
  窗口中部为外壳底色；透明度 0.4 时能透出桌面内容。

## v3.3.2（2026-09-15）

补上 Avalonia 宿主自 v3.2.0 起一直缺失的「无标题栏桌面挂件外壳」——之前几个版本看着仍是普通窗口，就是这里漏了。

### 🖥 内嵌到桌面：窗口不再带系统标题栏
- **根因（如实说明）**：v3.1.x 及更早的 Windows 安装包跑的是 WPF 宿主（MicaAgenda.App.exe），它的窗口是
  `WindowStyle=None` + `AllowsTransparency` 的无边框窗口；v3.2.0 起安装包换成 Avalonia 宿主
  （MicaAgenda.Desktop.exe），而 Avalonia 的 MainWindow 从来没设过无边框，于是窗口一直带着系统标题栏和
  右上角三个系统按钮 —— 看起来就是「普通窗口」，而不是贴在桌面上的挂件。此前几版的改动只摘掉了那三个
  系统按钮，标题栏本身还在，所以观感没变。这次是从根上修。
- **现在怎么做**：主窗口改为 `WindowDecorations=None` + 整窗透明，底色 / 22px 圆角 / 1px 细边框全部画在
  外层 Shell 上（与 WPF 宿主同一套外观）。窗口本身不再有标题栏，「×」「—」「□」在任何模式下都不存在。
- **拖动与缩放补回来**：没有系统边框，系统就不再提供「拖标题栏移动窗口」和「拖边缘缩放」，已在外壳上自绘：
  距边缘 8px 内缩放（含四角），其余位置拖动窗口；锁定窗口时两者都禁用；落在按钮 / 输入框 / 下拉框 / 滑块 /
  滚动条 / 日历格子 / 任务条目上时不误触发拖动。规则与 v3.1.x 的 WPF 宿主逐条对齐。
- **背景与透明度**：底色从窗口挪到外壳 Border，并保证窗口始终支持逐像素透明（圆角外沿能透出桌面），
  毛玻璃 / 亚克力 / 纯色 / 石墨等各背景模式外观保持不变。

### ✅ 测试
- 跨平台单测 79 例 + Windows 专测 27 例 = **106 例全部通过**。
- 另以窗口样式实测确认：窗口无 `WS_CAPTION`、无 `WS_SYSMENU`，嵌入桌面时仍保持 `WS_EX_NOACTIVATE` +
  `WS_EX_TOOLWINDOW`（任务栏无图标）。

## v3.3.1（2026-09-14）

紧急修复 v3.3.0 引入的「内嵌到桌面失效」回归，并补上「高优先级启动」的死链。

### 🩹 内嵌到桌面失效（v3.3.0 的回归，已修复）
- **窗口变回了「普通窗口」**：v3.3.0 为了在嵌入桌面下能打字，把窗口的「禁止激活」样式临时摘掉、
  顺手停掉了压底看门狗，指望提交 / 取消 / 失焦事件把状态收回来。只要有一次没收到（切窗口、
  托盘唤起、改尺寸都可能），窗口就永久停在「可激活、又不压底」的状态 —— 用户看到的就是
  内嵌失效、标题栏三个系统按钮全回来了。
- **现在怎么做**：看门狗永不停止，输入期间只是「这一 tick 先不压底」；什么时候收回来完全由
  看门狗自己轮询判断（键盘焦点是否还在行内输入框、窗口是否还在前台），外加 1.5 秒宽限期与
  5 分钟上限兜底。**任何一次判断漏掉都会在 200ms 内自愈**，不再依赖任何回调。
- **打字能力没有丢**：点加号 / 双击改名时，窗口仍会在输入期间临时放开激活，文字照常打得进去
  （已实测：嵌入状态下输入框收得到键盘输入，提交 / 取消后 0.2 秒内恢复沉底）。

### ❌ 「×」按钮在任何模式下都不会再回来
- 去掉标题栏系统按钮此前只在启动时做一次，而宿主在激活、改尺寸、DPI 变化、从托盘显示窗口
  等时机都会把窗口样式写回去 ——「叉号自己回来了」就是这么来的。现在改成 200ms 周期重放
  （样式已是目标值时是空操作，一次调用只多一两次 GetWindowLong），**开不开嵌入都一样**。

### 🔧 高优先级启动：计划任务指向旧宿主，开机变死链
- 旧版本用 WPF 宿主（MicaAgenda.App.exe）登记过 MicaAgenda_HighPriority 任务，而安装包升级时
  会把旧宿主文件删掉，任务却一直没人管 —— 于是「勾了高优先级启动却没作用」。现在登记前会先
  读回任务实际指向的程序，指向别的 exe 就用 /F 重建；未登记的照旧登记。
- 修正需要管理员权限：拿不到权限时如实回报「已登记，但仍指向旧路径；保存设置时授权即可修正」，
  不会把「任务已存在」误报成「需要管理员授权」，也不会每次保存设置都白弹一次 UAC。

### 🔧 关掉「内嵌到桌面」后窗口点不动
- 「禁止激活」样式此前只在嵌入时加、关闭时不撤，于是关掉嵌入后窗口会既沉在其他窗口之下、
  又无法被激活。现在关闭嵌入会撤掉该样式并把窗口唤回前台。

### ✅ 测试
- 跨平台单测 79 例 + Windows 专测 27 例 = **106 例全部通过**。
- 新增计划任务路径解析用例（任务 XML / 被按 8 位解码的 UTF-16 / XML 实体 / 空输入）与路径归一化比对用例。

## v3.3.0（2026-09-14）

与 my mindmap agent 的复习计划同步补齐「双向删除」，并修掉桌面端「点加号建不了任务」的问题。

### 🗑 双向删除（复习任务）
- **日历里删掉复习任务，对端同步删掉复习周期**：此前删除只在本地生效，下一次同步会按对端的
  复习计划把任务原样建回来 —— 用户看到的现象就是「删不掉」。现在界面右键删除、HTTP API、
  MCP 三条删除入口都会通知对端（新增 `POST /api/desk-calendar/delete`）。
- **对端离线时的删除不会丢**：删除先记进 `PendingReviewDeletions`（按「日期 + 去前缀标题」配对），
  推送失败就保留，之后每次同步自动重试；对端确认后记录自动清掉。记录会随数据一起落盘，
  重启程序也不会「复活」被删的任务。
- **待删记录参与比对**：同步时命中待删记录的任务既不重建、也不用对端状态去「修正」，
  更早版本同步留下的同一条残留会被顺手清掉。
- **记录自洽化**：保存前按「日期 + 标题」去重、丢掉日期/标题缺失的脏数据、清理超过 90 天的旧记录。

### 🖱 修复（桌面端点加号建不了任务）
- **加号点得动、却在输入框里打不了字**：点加号后抢焦点的逻辑取的是「格子里的第一个输入框」，
  而它其实是任务条目的编辑框（平时不可见），对不可见控件 `Focus()` 会被静默忽略，焦点始终落在
  加号按钮上。现在按 `DataContext` 精确定位本格的输入框，并在布局完成后重试一次。
  任务改名（右键「编辑」/ 双击）也一并补上了抢焦点。
- **嵌入桌面模式下输入框彻底打不了字**：嵌入桌面会给窗口加 `WS_EX_NOACTIVATE`（系统层面禁止激活），
  鼠标点击照常生效、键盘消息却永远送不进来。现在打开行内输入框期间临时放开激活并挂起压底看门狗，
  输入结束（提交 / 取消 / 失焦）立刻恢复「沉在桌面」，既打得进字也不会长期浮在别的窗口上面。

### 🧪 测试
- 新增 `ReviewSyncPlanner.Build` 待删记录的比对用例：命中即跳过创建、清掉残留、只影响自己的配对键、
  同标题的其他日期不受牵连。
- 新增 `MainViewModel.ReviewTaskDeleted` 用例：只有 `[MM复习]` / `[复习]` 任务会触发通知，用户自建任务不触发。
- 跨平台单测 79 项、Windows 专属实现 14 项，全部通过。

### 🔄 兼容
- **对端是旧版 my mindmap agent**：删除接口返回 404/405 时按「已处理」收尾（旧版本来就没有这个能力，
  反复重试没有意义），行为退回旧版 —— 日历上删掉的复习任务会在下一次同步被建回来。
- **对端是旧版日历**：不受影响，`/api/desk-calendar/delete` 只是新增接口。

## v3.2.3（2026-09-14）

针对「卡顿 / 内存 / 令牌暴露」的修补，并补齐与 my mindmap agent 同步的接口细节。

### 🐎 性能
- **桌面嵌入看门狗从 30 次/秒降到 5 次/秒**：看门狗只负责在窗口被系统抬到上面后重新压底，
  此前间隔 33ms，每个 tick 都要走两次窗口样式 / Z 序系统调用，纯属空转占用 UI 线程。
  现在 200ms，跟手程度不变，系统调用量降到 1/6。
- **同一天内不再重建整张日历**：整点刷新时钟时此前每分钟无条件重建一次格子 —— 月视图要遍历
  整条时间轴的每个格子并重绑任务，年视图更是一次性重建 12 个月共 365 个格子（还要先清空集合
  再逐个 Add，每次都引发一整轮 UI 刷新），而这些内容以「天」为粒度，结果与上一分钟完全相同。
  现在只有跨天才重建（今日高亮、日期行、「未完成 N 天」照常跟随），没跨天时只更新时钟文本。
- **窗口样式不再重复写**：嵌入桌面的「禁止点击激活」样式此前每个 tick 都无条件写一次
  `SetWindowLong`（每次都会触发窗口风格变更通知），现在与目标值一致时直接跳过。

### 🔐 安全
- **同步 Token 不再出现在 URL 里**：向 my-mindmap agent 取复习计划时 Token 此前拼在查询串
  （`?token=`）中，而 URL 会进入浏览器历史、Referer 与访问日志；现在改为标准
  `Authorization: Bearer` 请求头。推送任务状态时同样从请求体挪到请求头（请求体常被整体打印）。
  对端的 `/api/status` 与 `/api/desk-calendar/*` 都同时接受这两种方式，因此不需要同步升级对端。

### ✅ 测试
- 跨平台单测 72 例 + Windows 专测 14 例 = **86 例全部通过**。新增「同一天内不重建日历格子、
  跨天必须重建」用例。

### 说明
- 设置窗口里的「测试连接」按钮仍用 `?token=` 查询串（一次性人工诊断，且只发往用户自己填写的
  地址），这样对旧版 my-mindmap agent 也照样可用；所有自动同步流量都已改用请求头。

## v3.2.2（2026-09-13）

补 v3.2.1 的漏：Windows 安装包打的是 Avalonia 宿主，而「状态推回思维导图」这条链路此前只在 WPF 宿主里
实现过，所以从安装包装出来的版本反而是缺的。这一版把 Avalonia 宿主补成一致。

### 🐛 修复
- **勾选复习任务后不必再等一小时**：在日历里勾选 / 取消复习任务时，Avalonia 宿主以前不做任何推送，
  只能等下一次整点同步（对端轮询也是每小时一次），用户在思维导图里很久看不到状态变化。现在与 WPF 宿主
  一致：勾选后立刻把状态与「状态最后变更时间」推给 my-mindmap agent（对端按时间戳仲裁，谁新听谁的）。
  只对复习任务生效，未开启同步 / 没填 Token 直接跳过，失败也不影响日历本机操作。
- **设置里的「立即同步」不用先保存也能用**：Avalonia 宿主此前丢掉面板里刚填的地址 / Token，只读已保存的
  配置 —— 按「先点立即同步验证通了再保存」的顺序操作必然失败（提示未开启同步或未填写 Token）。现在与
  WPF 宿主一致，直接用面板里当前填写的地址 / Token 同步一次，且不受「启用同步」开关限制。
- 复习任务标题前缀判定（新版 `[MM复习]`、旧版 `[复习]`）收敛到 `ReviewTaskTitle` 一处，两个宿主与同步
  逻辑共用同一份定义，避免各写一份导致漏推或误推。

### ✅ 测试
- 跨平台单测 71 例 + Windows 测试 14 例 = **85 例全部通过**。新增「复习任务标题判定与同步逻辑一致」用例。
## v3.2.1（2026-09-13）

### 🔧 修复（移植自 v3.1.8 / v3.1.9 / v3.1.10）
v3.1.8 / v3.1.9 / v3.1.10 修掉的问题，此前只落在 WPF 那条线上（`fix/3.1.8` 分支），默认分支一直没有 ——
也就是说从 `main` 出包会把这些老问题原样带回去。现在全部移植过来，并把两个宿主要共用的部分下沉到
`MicaAgenda.Core`：

- **右上角「×」一律隐藏**（WPF 宿主）：此前只在勾了「锁定位置」时才隐藏。这是常驻桌面的小挂件，
  误点关掉后程序只是收进托盘，看起来就像「程序没了」。现在任何状态下都不显示，收起窗口走托盘菜单
  新增的「隐藏到托盘」，退出走托盘菜单的「退出」。（Avalonia 宿主此前已实现：Windows 上移除标题栏按钮。）
- **设置里「保存后立即生效」实际不生效**：设置窗口与主窗体共用同一个 `AppConfig` 实例，保存时是就地改写，
  主窗体再拿它当「旧值」比对就永远等于新值 —— 于是「嵌入桌面」「锁定位置」这些分支从来不会执行，
  只能重启程序才生效。现在保存前先留一份快照再比对（`AppConfig.Clone()`）。
- **「立即同步」在没先点保存时必然失败**：按钮只读已保存的配置，而用户通常是先点「立即同步」验证通了才点保存，
  于是总是提示「未开启同步或未填写 Token」。现在直接用面板里当前填写的地址 / Token 同步一次。
- **同步失败提示分不清原因**：401（Token 无效或已过期）、503（对端主界面没打开）、根本连不上，
  此前笼统报一句「无法连接」，现在分别给出能照着修的提示；「测试连接」会追加请求同步真正用到的
  `/api/desk-calendar/review-plan` 并显示复习任务条数。
- **同步失败后要干等一小时**：首次同步在启动 20 秒后，失败后只能等下一个小时周期。现在没通时 5 分钟就重试，
  通了再回到每小时。
- **复习任务集合也会对齐**：对端删掉复习周期、或对端本地映射丢失后残留下来的复习任务，以前会一直留在日历里；
  现在同步时会清掉对端计划里已不存在的任务，并清掉同一天同名的重复任务（对端始终是「复习任务存在与否」的权威）。
  计划为空时不动手，避免对端只是还没加载好复习计划就把本地复习任务误删。比对与仲裁逻辑抽成 `ReviewSyncPlanner`。
- **设置窗口太高、界面生硬**：窗口改成可自由缩放（带最小尺寸），卡片内边距与分组间距收紧，
  输入框 / 按钮 / 下拉框 / 复选框 / 滚动条统一换成圆角样式并补上悬停 / 按下 / 聚焦 / 只读 / 禁用状态，
  底部「取消 / 保存」常驻窗口底部。WPF 宿主 560×800 → 620×620；Avalonia 宿主 560×800（不可缩放）→ 660×620。
- **「高优先级开机启动」以前完全没作用**：勾上保存后既不报错也没有任何实际效果。两个独立原因：登记开机任务
  需要管理员权限，被拒绝后旧代码把失败信息吞掉了；就算任务侥幸登记成功也启动不了程序 —— 任务动作里用的是
  `Start-Process -Priority High`，而 `Start-Process` 根本没有 `-Priority` 参数。现在改为：高优先级由程序自己
  在启动时设置（立刻生效、不需要管理员），开机任务只负责「拉起程序」；登记会真的请求管理员授权，失败或取消
  都会如实说明并把选项回退，不留下「勾着但没生效」的假象。

### ✨ 新增
- **Avalonia 宿主补上「开机自启动 / 高优先级启动」**：此前设置窗口只写一句「开机自启动为 Windows 专属能力，
  请在 Windows 版（WPF 宿主）中配置」，而 Windows 安装包打的正是 Avalonia 宿主 —— 等于这两项在 Windows 上
  直接消失。现在 `AutoStartService` 与 `HighPriorityStartupService` 下沉到 `MicaAgenda.Core` 由两个宿主共用，
  Avalonia 设置窗口补上这两项与真实的任务登记状态；非 Windows 平台整块隐藏，不给用户「点了没反应的开关」。

### 🐛 修复（安装包 / 开机自启动）
- **安装包里的「清理旧版本残留文件」从未执行过**：`setup.iss` 里那段删除旧文件的过程叫
  `CurInstallBeforeInstall`，而 Inno Setup 并没有这个事件 —— 它从来没有被调用过，所以从 WPF 版（v3.1.x）
  升上来的安装目录里一直留着 `MicaAgenda.App.exe` / `MicaAgenda.App.dll` 与 `PresentationFramework*.dll`、
  `*_cor3.dll` 等 WPF 专属依赖。现在改成真正的事件调用点（`CurStepChanged` 的 `ssInstall`，在关掉旧进程
  之后执行），只保留卸载器必需的 `unins000.*`，其余文件由 `[Files]` 重新铺一遍；顺带去掉路径拼接里多写的
  一层反斜杠。已用 Inno Setup 6.7.3 编译并实际跑过一次安装：安装目录里的旧宿主文件会被清掉，而安装到
  用户数据目录（目录名不是 `desktop_todo_Calendar`）时会拒绝清理。
- **清理的自我保护条件会把自己挡掉**：安装包免管理员（`PrivilegesRequired=lowest`），`{app}` 实际落在
  `%LOCALAPPDATA%\Programs\desktop_todo_Calendar`，而旧代码「路径里含 AppData 就放弃清理」正好命中 ——
  纵使被调用也会直接退出。现在改为认安装目录的最后一级目录名。
- **开机自启动有两个互相看不见的注册表值名**：安装脚本勾「开机自启动」写的是 `desktop_todo_Calendar`，
  程序内「开机自启动」用的是 `MicaAgenda`。结果是：安装包写的这条程序既看不到（设置里显示未开启）也删不掉
  （关掉自启后照样开机启动），两条同时存在还会让同一个程序开机被拉起两次。现在程序会同时识别两个值名，
  保存后统一收敛为 `MicaAgenda` 一条。
- **升级后开机自启动指向旧宿主 exe**：启动项里存的是绝对路径，装了新版（宿主从 `MicaAgenda.App.exe` 换成
  `MicaAgenda.Desktop.exe`）之后旧路径已不存在 —— 表现就是「勾着开机自启动，开机后没反应」。现在用户在设置里
  开着自启时，程序启动会把启动项重新指向当前这份 exe。

### ✅ 测试
- 跨平台单测 65 例 + Windows 测试 14 例 = **79 例全部通过**（迁移前基线为 45 例）。
  新增覆盖：同步比对 / 仲裁（新建、状态一致、两端各自更新 LWW、时间戳打平、重复清理、孤儿清理、空计划保护）、
  开机任务命令行拼装（路径引号 / 空格路径 / 删除参数）、开机自启动旧值名的识别与清理。

### 📌 说明
- Windows 专属能力（注册表开机自启、schtasks 高优先级自启）需要在 `MicaAgenda.Core` 编译，
  因此 Core 增加了 `Microsoft.Win32.Registry` 与 `System.Security.Principal.Windows` 两个包引用；
  调用点一律先判断 `OperatingSystem.IsWindows()`，非 Windows 平台不会走到。
- 安装包只清安装目录的顶层文件：语言子目录（`cs` / `de` / `zh-Hans` 等）里 WPF 时代残留的资源 DLL 不会被删除，
  新版不会加载它们，只占少量磁盘。
- 安装包「开机启动」勾选项写的仍是注册表值名 `desktop_todo_Calendar`（沿用历史命名，避免影响旧版本卸载时的
  清理），程序识别到它之后会在下次保存设置时收敛为 `MicaAgenda`。

## v3.2.0（2026-09-12）

### ✨ 新增
- **跨平台支持（Windows / macOS / Linux）**：新增基于 Avalonia 的跨平台宿主 `MicaAgenda.Desktop`，数据层与服务层抽取为 `MicaAgenda.Core`，两个宿主（WPF / Avalonia）共用同一份实现。CI 四路并行构建四平台产物。
- **右侧任务面板**：今日任务（未完成 / 已完成分组 + 快速添加 + 清空）与「本周任务完成情况」三组（逾期 / 未完成 / 已完成，各带批量删除与标记按钮，破坏性操作二次确认）。
- **月视图无限滚动**：滚动到顶部 / 底部自动向前后扩展时间轴，并做偏移补偿，视口不再跳动。
- **视觉对等**：12 种背景模式改用跨平台透明度（Mica / Acrylic / Blur / 透明 / 纯色 + 着色）、透明度滑块、按视图（月 / 周 / 年）记忆窗口位置与尺寸。
- **设置窗口全量移植**：HTTP API、MCP、my-mindmap 同步、定时提醒、定时报告、自动备份、节假日数据、桌面行为、一键重置。
- **后台服务接入 Avalonia 宿主**：MCP、任务 API、定时提醒、自动备份、定时报告、思维导图复习双向同步。

### 🐛 修复
- **任务改动不落盘**：Avalonia 宿主此前只加载不保存，所有改动关闭即丢；现补防抖自动保存 + 退出前兜底落盘。
- **无限滚动视口跳动**：向上扩展时间轴时按 extent 增量回补滚动偏移，并加冷却避免连锁触发。
- **右键菜单被吞**：面板条目 / 本周行的 PointerPressed 只响应左键，右键正常弹出上下文菜单。
- 复制 Token / Skill 文档 / MCP 配置改用可全选文本框（规避跨平台剪贴板 API 差异）。

### 📌 说明
- Linux 桌面嵌入（置底模式）本次未实现；Windows / macOS 支持。

## v3.1.7（2026-09-07）

### ✨ 新增
- **与 my-mindmap agent 复习计划双向同步**：
  - 新增 `MindMapReviewSyncService`，每小时自动对齐一次（启动 20 秒后首跑），把日历任务状态与对端复习周期状态按"最后变更时间"仲裁对齐。
  - 仅同步标题带 `[MM复习]`（旧版 `[复习]`）前缀的任务 —— 由 my-mindmap agent 的"复习计划"生成；用户或其他 AI 自己建的日历任务不参与同步。
  - 设置面板新增「立即同步」按钮与「my mind map agent 本地 HTTP 地址」输入，测试连接改用配置的地址。
- **冲突仲裁升级为时间戳 LWW**：每个任务新增 `UpdatedAt`（完成/取消完成都会刷新），同步服务对比本地与对端的"状态最后变更时间"，最新者胜出。
  - 场景：用户在 mindmap 端 10:00 勾选完成、10:01 误触取消 —— 取消的时间戳更新，会把"未完成"作为最新状态推到日历，不会把误触的"已完成"留在对端。
- **任务 DTO 新增 `updatedAt` 字段**：MCP `query_tasks` 返回的每个任务都带状态时间戳，方便对端做仲裁。

### 🐛 修复
- **剪贴板复制 0x800401D0 卡顿**：「复制 Skill 文档」「复制 MCP 配置 JSON」「复制 Token」之前偶发 `CLIPBRD_E_CANT_OPEN`。改为 Win32 原生 OpenClipboard + SetClipboardData，带 10/20/40/80/120/160/200/250/300/350 ms 退避重试；原生路径失败时回退 WPF DataObject 再试 5 次；失败时弹可手动 Ctrl+C 的复制窗口（不再只弹 0x800401D0 报错）。

### 🔧 优化
- 老任务没有 `UpdatedAt`：保存前统一 `Normalize` 兜底为 `CompletedAt ?? CreatedAt`，保证时间戳仲裁永远有可比依据。
- 同步结果附带"新增 / 更新 / 拉取 / 删除"分项计数与最近一次同步时间，方便在设置面板观察。

## v3.1.5（2026-09-01）

### 🐛 修复
- **设置保存必弹错误框**：`SaveCore()` 被历史代码调用了两次，第一次已关闭窗口，第二次再设 `DialogResult` 抛
  "只能在创建 Window 并且作为对话框显示之后才能设置 DialogResult"。现在只调用一次。
- **嵌入桌面保存后立即生效**：之前勾选"嵌入桌面/锁定"后提示"重启才生效"，用户勾完没反应误以为坏了。
  现在保存即应用（开启立即沉底并启动看门狗，取消立即恢复普通窗口），无需重启。
- 看门狗 Tick 事件只挂载一次，避免嵌入开关反复切换时累积多个定时处理器。

### 🎨 界面
- "本周任务完成情况 → 已完成"组的"标记为未完成"按钮图标由 ✗（橙色）改为 ↺（蓝色），
  与同组删除按钮 ✕（红色）明显区分，避免误点删除。