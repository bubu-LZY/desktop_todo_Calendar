# Changelog

## v3.1.9（2026-09-13）

### 🔧 优化
- **设置窗口不再那么高**：窗口从 560×800 收缩到 620×620，同时改成可自由缩放（带最小尺寸限制），小屏笔记本上不会再顶出屏幕。卡片内边距与分组间距一并收紧，整页内容高度也比之前更短。
- **设置界面不再生硬**：改成浅灰底 + 白色圆角卡片的层次，分组标题前加一条细色条；输入框、按钮、下拉框、复选框、单选框、滚动条统一换成圆角样式，并补上悬停 / 按下 / 聚焦 / 只读 / 禁用的状态反馈。
- **底部按钮常驻**：取消 / 保存 固定在窗口底部，内容再长也不会被滚走。

## v3.1.8（2026-09-13）

### 🐛 修复
- **右上角「×」一律隐藏**：之前只有在设置里勾了「锁定位置」才会隐藏，其他状态下一直露着。这是常年挂在桌面上的小挂件，误点关掉后程序只是收进托盘，看起来就像"程序没了"。现在任何状态下都不显示；收起窗口走托盘菜单新增的「隐藏到托盘」，退出走托盘菜单的「退出」。
- **设置里"保存后立即生效"实际不生效**：设置窗口与主窗体共用同一个 `AppConfig` 实例，保存时是就地改写，主窗体再拿它当"旧值"去比对就永远等于新值 —— 于是「嵌入桌面」「锁定位置」这些分支从来不会执行，只能重启程序才生效。现在保存前先留一份快照再比对（新增 `AppConfig.Clone()`）。
- **「立即同步」在没先点保存时必然失败**：按钮只读已保存的配置，而用户通常是先点「立即同步」验证通了才点保存，于是总是提示"未开启同步或未填写 Token"。现在直接用面板里当前填写的地址 / Token 同步一次，不受开关限制。
- **同步失败提示分不清原因**：401（Token 无效或已过期）、503（对端主界面没打开）、根本连不上，之前笼统报一句"无法连接"。现在分别给出能照着修的提示。
- **「测试连接」测不出真正的问题**：之前只请求 `/api/status`，对"能连上、但取不到复习计划"（对端主界面没打开）完全无感。现在会再请求同步真正用到的 `/api/desk-calendar/review-plan`，并显示复习任务条数。
- **同步失败后要干等一小时**：首次同步在启动 20 秒后，失败后只能等下一个小时周期。现在没通时 5 分钟就重试，通了再回到每小时。

### 🔧 优化
- **复习任务集合也会对齐**：对端删掉复习周期、或对端本地映射丢失后残留下来的复习任务，以前会一直留在日历里；现在同步时会清掉对端计划里已经不存在的任务，并清掉同一天同名的重复任务（对端始终是"复习任务存在与否"的权威）。计划为空时不动手，避免对端只是还没加载好复习计划就把本地复习任务误删。
- **比对与仲裁逻辑抽成 `ReviewSyncPlanner` 并补单测**：新建、状态一致、两端各自更新（LWW）、时间戳打平、重复清理、孤儿清理、空计划保护等场景都有覆盖。

### 📌 说明
- 新增 CI 自动打包发布（`.github/workflows/release-build.yml`，推 `v*` 标签触发），不再依赖本地手工编译安装包。
- 安装脚本会顺手清掉安装目录里上一版遗留的 Avalonia 跨平台宿主文件（`MicaAgenda.Desktop.*` / `MicaAgenda.Core.*` / `Avalonia*.dll` / Skia / HarfBuzz 等）：它们不属于本安装包，留着白占空间，也容易让人搞不清装的是哪个版本。
- 文档里过期的默认端口改为实际值：MCP `17804`、HTTP API `17803`（此前文档写的 `17802` / `17801` 是旧值）。

## v3.1.7（2026-09-07）

### ✨ 新增
- **与 my-mindmap agent 复习计划双向同步**：
  - 新增 `MindMapReviewSyncService`，每小时自动对齐一次（启动 20 秒后首跑），把日历任务状态与对端复习周期状态按"最后变更时间"仲裁对齐。
  - 仅同步标题带 `[MM复习]`（旧版 `[复习]`）前缀的任务 —— 由 my-mindmap agent 的"复习计划"生成；用户或其他 AI 自己建的日历任务不参与同步。
  - 设置面板新增「立即同步」按钮与「my mind map agent 本地 HTTP 地址」输入，测试连接改用配置的地址。
- **冲突仲裁升级为时间戳 LWW**：每个任务新增 `UpdatedAt`（完成/取消完成都会刷新），同步服务对比本地与对端的"状态最后变更时间"，最新者胜出。
  - 场景：用户在 mindmap 端 10:00 勾选完成、10:01 误触取消 —— 取消的时间戳更新，会把"未完成"作为最新状态推到日历，不会把误触的"已完成"留在对端。
- **任务 DTO 新增 `updatedAt` 字段**：MCP `query_tasks` 返回的每个任务都带状态时间戳，方便对端做仲裁。

### 🐛 修复
- **剪贴板复制 0x800401D0 卡顿**：「复制 Skill 文档」「复制 MCP 配置 JSON」「复制 Token」之前偶发 `CLIPBRD_E_CANT_OPEN`。改为 Win32 原生 OpenClipboard + SetClipboardData，带 10/20/40/80/120/160/200/250/300/350 ms 退避重试；原生路径失败时回退 WPF DataObject 再试 5 次；失败时弹可手动 Ctrl+C 的复制窗口（不再只弹 0x800401D0 报错）。

### 🔧 优化
- 老任务没有 `UpdatedAt`：保存前统一 `Normalize` 兜底为 `CompletedAt ?? CreatedAt`，保证时间戳仲裁永远有可比依据。
- 同步结果附带"新增 / 更新 / 拉取 / 删除"分项计数与最近一次同步时间，方便在设置面板观察。

## v3.1.5（2026-09-01）

### 🐛 修复
- **设置保存必弹错误框**：`SaveCore()` 被历史代码调用了两次，第一次已关闭窗口，第二次再设 `DialogResult` 抛
  "只能在创建 Window 并且作为对话框显示之后才能设置 DialogResult"。现在只调用一次。
- **嵌入桌面保存后立即生效**：之前勾选"嵌入桌面/锁定"后提示"重启才生效"，用户勾完没反应误以为坏了。
  现在保存即应用（开启立即沉底并启动看门狗，取消立即恢复普通窗口），无需重启。
- 看门狗 Tick 事件只挂载一次，避免嵌入开关反复切换时累积多个定时处理器。

### 🎨 界面
- "本周任务完成情况 → 已完成"组的"标记为未完成"按钮图标由 ✗（橙色）改为 ↺（蓝色），
  与同组删除按钮 ✕（红色）明显区分，避免误点删除。
